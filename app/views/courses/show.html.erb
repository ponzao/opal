<h2><%= @course.name %></h2>
<p><%= @course.description %></p>
<p>
<!--<%= link_to 'Edit', edit_course_path(@course) %> |-->
<%= link_to 'Back', courses_path %>
</p>

<ul>
    <% @course.course_instances.each do |ci| %>
        <li><%= link_to ci.name, course_instance_url(ci) %></li>
    <% end %>
</ul>
